# ============================================================

MONGODB_URI="mongodb://triptotravelsofficial_db_user:HbhZsj4sM0aU2TOP@cluster0-shard-00-00.x4so0us.mongodb.net:27017,cluster0-shard-00-01.x4so0us.mongodb.net:27017,cluster0-shard-00-02.x4so0us.mongodb.net:27017/triptotravel?ssl=true&replicaSet=atlas-xxxxx-shard-0&authSource=admin&retryWrites=true&w=majority"
# Generate new: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456
JWT_EXPIRES_IN=7d

# --- Email (Gmail: use App Password for Triptotravelsofficial@gmail.com) ---
# Works for both Triptotravels.com and Triptotravels.in
SMTP_MAIL_FROM=Triptotravelsofficial@gmail.com
SMTP_APP_PASSWORD=axsk axzi ocdt ixbk
# Optional: base URL for email links when not from request (e.g. https://triptotravels.com or https://triptotravels.in)
SITE_URL=https://triptotravels.com

# --- Razorpay (required for real payments) ---
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# --- Travel APIs (optional – add when you have keys; backend uses them automatically) ---
# Amadeus – flights + hotels (test = cached, production = real-time)
AMADEUS_API_KEY=
AMADEUS_API_SECRET=
# For real-time data use production URL (default below = test/cached):
# AMADEUS_BASE=https://api.amadeus.com

# Skyscanner – flights (affiliate; you earn on bookings)
SKYSCANNER_API_KEY=

# KAYAK – flights + hotels (affiliate)
KAYAK_API_KEY=
KAYAK_AFFILIATE_ID=

# Booking.com – hotels (affiliate)
BOOKING_API_KEY=
BOOKING_AFFILIATE_ID=

# --- Admin (first admin/superadmin account) ---
ADMIN_EMAIL=Triptotravelsofficial@gmail.com
ADMIN_PASSWORD=
# Set to true to create superadmin instead of admin
ADMIN_SUPERADMIN=false

# --- Business model (stored in DB; optional override for provider) ---
# TRAVEL_PROVIDER=mock|amadeus|skyscanner|booking

# --- Image storage (marketplace hotel/room images). Use local path or KVM4 mount. ---
# STORAGE_PATH=./storage
# Or absolute path to your KVM4 storage volume, e.g.:
# STORAGE_PATH=/mnt/kvm4
# KVM4_STORAGE_PATH=/mnt/kvm4
